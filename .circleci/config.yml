version: 2.1

jobs:
  build:
    docker:
      - image: node:slim
    steps:
      - checkout
      - run: echo "Hello World!"
      - setup_remote_docker:
          version: 20.10.14
      - run: docker info
      
workflows:
  test_workflow:
    jobs:
      - build
      
